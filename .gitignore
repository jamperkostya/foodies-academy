# Docker
mysql-init/*.sql.bak

# WordPress core ships inside the official image and is never versioned.
# Only wp-content (themes/plugins/custom code) is tracked;
# uploads are user-generated media and don't belong in the repo.
wp-content/uploads/

# OS
.DS_Store
